# Decision Log

## D001 - Prepared starter route
The repository is already initialized. INIT remains as a reproducible contract but must not be rerun against this folder.

## D002 - Deterministic-first architecture
The no-key deterministic engine is the default so testing, teaching, and demos remain reproducible.

## D003 - Synthetic hypothesis language
All model outputs are described as synthetic hypotheses, not real reactions or clinical predictions.

## D004 - Multidimensional support profiles
The implementation replaces a single functioning-level field with communication, sensory, routine, social, trigger, and support dimensions.

## D005 - Structured-output API boundary
Optional OpenAI calls use the Responses API, Pydantic structured outputs, environment-selected model, and `store=False`.

## D006 - Audit environment and gitignore hygiene (2026-08-03)
The one-time audit confirmed the prepared starter. Runtime environment is a Python 3.11+ `.venv`; the machine's system `python3` (3.9.6) is below `requires-python` and is not used. `*.egg-info/` was added to `.gitignore` so editable-install build artifacts are never committed. Two non-blocking observations were recorded: `config/app_config.toml` is reference-only (runtime uses env vars), and the AI-mode default model `gpt-5.6-terra` is a placeholder to confirm before enabling AI mode.

## D007 - Scope and claims boundary frozen at S01 (2026-08-03)
S01 froze the project boundary in version-controlled documentation so later units implement against a fixed target. The research question, user-visible disclaimer, in-scope deliverables, exclusions, and success criteria are recorded in `docs/AuthoritativePlan.md` and `docs/ResearchEthics.md` and surfaced in `README.md`. The freeze is prose-level only: enforcing the claims boundary and synthetic labelling as code/tests is deferred to a later unit (e.g., S06 contracts/tests or S13 provenance). Deterministic no-key operation is unchanged.

## D008 - Evidence base established at S02 (2026-08-03)
S02 created the traceable evidence base: 8 real sources (NIMH, Cochrane music therapy, Cochrane sound-based interventions, Belmont Report/OHRP, UNESCO AI ethics, ASAN, OpenAI docs, Streamlit docs) logged in `docs/evidence_table.csv`; 8 background claims (C1–C8) mapped to sources in `docs/claim_ledger.md`; and a truthful non-systematic search log added to `docs/LiteratureReviewProtocol.md`. No citation or result was invented: unverified exact figures (sample sizes, effect sizes, certainty ratings, deep-link URLs) are recorded as "verify before public release." The review is student-curated and non-systematic and cannot support clinical claims.

## D009 - Persona schema finalized at S03 (2026-08-03)
S03 finalized the multidimensional persona schema in `src/ai_music_therapy/models.py`: a new `SupportProfile` (communication, sensory, routine, social) replaced the loose `support_profile: dict[str,str]`; `model_config = ConfigDict(extra="forbid")` was added to `Persona`, `SupportProfile`, and `SensoryProfile`; and behavioural lists and key text fields require `min_length=1`. Together these enforce multidimensional support and make a single functioning-level label unconstructible (covered by tests). `docs/persona_design.md` documents the schema, the five-dimension mapping, neurodiversity safeguards, and stereotype/representation limitations. The existing five fictional profiles already satisfy the hardened schema (no data change), and the deterministic simulator (which reads only `persona_id` and `sensory_profile`) is unaffected.

## D010 - Music ontology and scenario rubric defined at S04 (2026-08-12)
S04 defined the controlled music vocabulary and the five support scenarios as explicit, centralized data. `config/music_ontology.json` declares the allowed genre/bpm(40–120)/volume/instrument/tonality/duration(60–300)/lyrics values plus five scenarios, each with a non-clinical software outcome rubric, favorable direction, stop conditions, and a clinical note; `docs/scenario_rubric.md` is the human-readable spec. The Pydantic schema remains the runtime-enforced source of truth; a new test keeps the JSON and the schema's `Literal`/`Field` constraints in agreement so they cannot drift. Stop conditions mirror the deterministic simulator's existing `safety_flags` (simulated anxiety >= 8 and volume == high). No rubric is described as a validated clinical measure.

## D011 - Experiment design and preregistration frozen at S05 (2026-08-12)
S05 froze the experiment before any trials run (S16). `config/trial_matrix.csv` defines exactly 75 cells = 5 personas × 5 scenarios × 3 music variants, fully crossed, with the 3 variants identical to those in `scripts/run_batch_demo.py`. `docs/preregistration.md` records the non-clinical software hypotheses (H1–H4), the independent/dependent/controlled/provenance variables, and frozen exclusion/reporting rules. `docs/analysis_plan.md` specifies a descriptive-only analysis (`composite_score` primary; per-dimension secondary; no inferential or clinical claims). The matrix is validated against the fixtures, the model, and the batch runner, but is not yet enforced by a committed guard test — a future unit (S06 or S16) should add one so the batch runner cannot drift from the preregistered matrix.

## D012 - Repository contracts, schemas, CI, and tests strengthened at S06 (2026-08-12)
S06 strengthened package contracts without adding UI features. All 26 pre-existing `ruff` errors were fixed (6 auto-fixes + 20 manual line wraps; UI edits were formatting-only) so `ruff check src tests scripts` is green and the CI gate passes. `model_config = ConfigDict(extra="forbid")` was added to `TimeStage`, `ReactionOutput`, and `TrialRecord`, so every data model now rejects invalid/extra records. `scripts/smoke_test.py` was extended to run one deterministic `simulate()` end-to-end and assert the 75-cell matrix is present. `.github/workflows/ci.yml` now sets `env: AI_MUSIC_APP_MODE: deterministic` so CI never depends on a live API. Four tests were added (18 total): the trial-matrix contract, ReactionOutput/TrialRecord extra-field rejection, a repository trial round-trip, and an ai-client no-key guard (monkeypatched so it fires before any network call). The matrix is now guarded as a contract; the remaining follow-up (for S16) is to make `scripts/run_batch_demo.py` read `config/trial_matrix.csv` so runner and preregistration share one source of truth.

## D013 - Streamlit shell, global disclaimer, and empty states at S07 (2026-08-12)
S07 implemented the multipage Streamlit shell with no new analytics or AI logic. `app.py` uses `st.navigation` for the Lab/Research pages and renders a global disclaimer in the sidebar on every page (plus engine-mode/prompt-version transparency). `home.py` is a landing page with the prominent frozen disclaimer, the persona→music→synthetic-response→analysis loop, a how-to guide, and a synthetic statement. `trial.py` shows the disclaimer on the results surface and surfaces existing `safety_flags` as a stop-condition warning. `methods.py` adds the disclaimer and a list of frozen project documents. `personas.py` and `dashboard.py` were unchanged (they already had useful empty states and synthetic labelling). All pages were verified headlessly via `streamlit.testing.v1.AppTest` (no exceptions; empty states present; disclaimer visible). Pre-existing trial-execution and dashboard-chart code was preserved non-destructively for refinement in S13/S14/S15; full browser-level UI verification is deferred to the manual demo in S18.

## D014 - Local credentials support and AI-mode probe (2026-08-12, between sessions)
Between sessions the user added real credentials to `.env.local`. Two safety/infrastructure changes were made: (1) `.gitignore` now covers `.env` and `.env.*` (with `!.env.example`) so `.env.local` can never be committed; (2) `src/ai_music_therapy/config.py` now loads `.env.local` with `override=True` (after `.env`) so local credentials are actually read (previously only `.env` was loaded). `.env.example` documents the optional `OPENAI_BASE_URL` for OpenAI-compatible providers. A live AI-mode probe against the user's GLM setup (`OPENAI_MODEL=glm-5.2`, `OPENAI_BASE_URL=…/v4/chat/completions`) returned 404 `…/v4/chat/completions/responses`: `ai_client.py` uses the OpenAI Responses API, which GLM's compatibility layer does not support, and the base URL should be the API root. This is deferred to **S10**. Deterministic mode is unaffected and remains the default. The real key was never printed, staged, or committed; it lives only in the gitignored `.env.local`.

## D015 - Persistence switched from SQLite to PostgreSQL (2026-08-19, between sessions)
The user requested, ahead of S08, that all persistence move from SQLite to PostgreSQL because the app will be deployed to Vercel. Changes: `docker-compose.yml` adds a local `postgres:16-alpine` service (dev-only credentials, deliberately non-secret); `src/ai_music_therapy/config.py` exposes `DATABASE_URL` (default `postgresql://postgres:postgres@localhost:5432/mt_simulator`); `src/ai_music_therapy/repository.py` was rewritten on psycopg 3 with lazy per-operation connections (module import never needs a reachable DB), preserving the same tables, the `CHECK (synthetic = 1)` constraints, the FK, full provenance JSON payloads, and the upsert semantics; the `trials.seed` column uses `BIGINT` because the deterministic sha256-derived seed exceeds 32-bit `INTEGER`; `seed_demo.py` and the three UI pages now pass `settings.database_url`; tests run against a dedicated `mt_simulator_test` database (created on demand, truncated per test) and skip with an explicit message when no PostgreSQL is reachable; CI adds a `postgres:16` service container. The prior `data/local/app.db` contained only seeded synthetic personas and demo trials, so no data migration was written - personas are re-seeded via `python -m ai_music_therapy.seed_demo`. The S08 prompt's "SQLite" wording is superseded by this decision: S08 proceeds against PostgreSQL. Caveat recorded for the user: Streamlit (a long-running WebSocket server) cannot run on Vercel's serverless platform; a Render/Railway/Fly/Streamlit-Community-Cloud host with a hosted PostgreSQL (Neon/Supabase/Vercel Postgres) is the realistic deployment path, and the PostgreSQL switch is correct for all of them. Local runtime environment: Docker via Colima 0.10.3 (`--vm-type qemu`; the default VZ runtime failed to boot on this Mac) with the docker daemon inside the VM configured to use the host proxy for image pulls (see EnvironmentRecord).

## D016 - Deployment target chosen: Streamlit Community Cloud + Neon PostgreSQL (2026-08-19, between sessions)
The student accepted the deployment caveat raised with D015 and chose the target stack ahead of S18: the app will run on Streamlit Community Cloud (free, purpose-built for Streamlit; Vercel-style serverless is impossible because Streamlit is a long-running WebSocket server), and the hosted database will be Neon PostgreSQL (free tier; fits the existing psycopg 3 / DATABASE_URL layer with no code change). A step-by-step deployment manual, `docs/DeploymentGuide_StreamlitCloud_Neon.md`, was drafted now at the student's request but is explicitly marked as a draft: nothing in it has been executed, and S18 must perform the deployment, collect evidence under `evidence/S18/`, and update the guide and this record with actuals. Known code work flagged for S18 by the guide: (a) add `-e .` to `requirements.txt` so Streamlit Cloud can import the `src/`-layout package; (b) decide between advanced-settings env vars vs `st.secrets` for `DATABASE_URL` on the cloud (config.py currently reads os.getenv only); (c) seed personas into Neon once. Neon free-tier caveats (auto-suspending compute causing cold starts, limited PITR history) are recorded in the guide and must be disclosed in the portfolio/demo rather than hidden.

## D017 - S08 accepted: persistence and synthetic fixtures on PostgreSQL (2026-08-20)
S08 was executed against PostgreSQL per D015 (the prompt's SQLite wording was superseded by the between-session switch). The implementation itself (repository initialization, persona seed, trial persistence, provenance JSON) already existed from D015; this session verified it against the S08 acceptance criteria and closed four gaps with new tests in `tests/test_repository.py` (23 total): (1) `Repository.initialize()` is idempotent (re-running `CREATE TABLE IF NOT EXISTS` is safe); (2) `seed_demo.main()` is idempotent end-to-end (running it twice leaves exactly 5 personas with 5 distinct ids - verified both in the test DB and live against `mt_simulator` in `evidence/S08/check_outputs.txt`); (3) the synthetic-only governance guard is enforced by the database itself, not just application code (inserting a row with `synthetic = 0` raises `CheckViolation`); (4) a persisted trial's `payload_json` round-trips every provenance key (engine/model/prompt/seed/timestamp/disclaimer) and the indexed columns match the JSON payload. "Local DB files remain ignored" holds trivially: the database is a docker volume, and `data/local/` remains gitignored (verified with `git check-ignore`). One limitation for the student to be able to explain: the repository opens a fresh connection per operation - simple and safe for a single-user demo, but not pooled, so it would need connection pooling before any concurrent multi-user deployment (relevant when S18 deploys to Streamlit Community Cloud + Neon).

## D018 - S09 accepted: deterministic reference simulator verified and documented (2026-08-20)
S09 verified the pre-existing deterministic rule engine and closed its two gaps: documentation and systematic testing. `docs/deterministic_model.md` (new) documents the determinism pipeline (sha256-stable seed -> seeded bounded jitter -> clamped rule arithmetic), summarizes every score rule, the complete start/middle/end time-stage contract, the two safety-flag stop conditions, and states prominently that the rules are fictional and non-clinical (the acceptance wording). `tests/test_deterministic_simulator.py` grew from 1 test to 8 test functions / 154 test cases by parameterizing over every one of the 75 preregistered matrix cells: per-cell reproducibility, seed == stable_seed(...), all four scores clamped to [1,10], attention within [15, duration], exactly three ordered complete stages with in-range scores, synthetic=True, non-empty uncertainty note, plus seed sensitivity to each input dimension and flag-condition equivalence (distress flag iff anxiety >= 8, volume flag iff volume == high). No engine code changes were needed; the engine already satisfied the objective. Discovered fact recorded: the preregistered matrix contains no high-volume cells (50 low / 25 medium), so the volume flag is tested with a constructed input. Full suite 176 passed; evidence in `evidence/S09/check_outputs.txt`. Limitation for the student to state: the rule coefficients are invented for teaching with no empirical basis, and only three sensory dimensions influence the arithmetic while communication/routine/social dimensions shape the persona schema only.

## D019 - S10 accepted: AI client on the Responses API with a strict validation boundary (2026-08-20)
S10 rebuilt `src/ai_music_therapy/ai_client.py`. Provider context changed before the unit: the user's `.env.local` now targets Volcano Ark (`ark-code-latest` at `.../api/plan/v3`), and a live probe showed this endpoint supports `POST /responses`, so the Responses API works directly and the D014 GLM blocker (chat-completions-only) no longer applies. Design: (1) requests use `client.responses.create(..., text={"format": {"type": "json_object"}}, store=False)` - Responses API with storage disabled, per acceptance; (2) the model is prompted with the exact field list and constraints; (3) because OpenAI-compatible providers do not enforce JSON schemas server-side (observed live: extra keys, a bare string where a list was expected), a sanitizer removes only semantically-neutral shape drift (unknown keys dropped, string wrapped to one-element list, start/middle/end reordered); (4) the result must strictly Pydantic-validate as ReactionOutput - out-of-range scores, wrong types, and missing fields are rejected, never clamped (tested); (5) one retry with validation errors fed back, then RuntimeError. `config.py` gained an explicit `openai_base_url` setting (test-injectable). `tests/test_ai_client.py` grew from 1 to 6 tests, all mocked - no live call in automated tests. One live end-to-end call was made as manual evidence and validated successfully. `docs/ai_boundary.md` (new) documents the hard boundary rules, provider compatibility, and limitations. Deterministic no-key default operation unchanged (full suite 182 passed, ruff clean). Known limitation for the student: AI output is not reproducible (no seed; TrialRecord keeps seed None for AI trials), and `store=False` is a provider request that cannot be independently verified from the client. The earlier partial key echo into the local session transcript (grep during S10 setup) was flagged to the user with a recommendation to rotate if concerned; the key remains only in gitignored `.env.local`.
