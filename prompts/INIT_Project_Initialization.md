# INIT - Project Initialization

## Route Warning
Use this prompt exactly once only in a genuinely empty workspace. The packaged starter is already initialized, so **do not run INIT against it**.

## Objective
Materialize the approved `ai-music-therapy-simulator` structure and foundational control files without implementing S01-S18.

## Required Initialization
Create README, TASKS, STATUS, SESSION_STATE, prompts, docs, config, public synthetic fixture, package placeholder, smoke test, ignore rules, environment placeholders, dependency manifests, and CI scaffold matching the approved tree.

## Verification
Print the tree, parse JSON/TOML, validate the five synthetic persona fixtures, run only the package import/smoke test, scan for secrets/private data, and report files created or preserved.

## Do Not Do
Do not generate research results, collect real-person data, implement S01-S18, call live APIs, create deployment claims, overwrite existing files, or commit secrets/private information.

## Stop Condition
Stop after initialization passes. Do not begin the workspace audit or S01.
