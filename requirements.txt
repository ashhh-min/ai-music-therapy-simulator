# Streamlit Community Cloud installs this file. `-e .` installs the package
# itself (src/ layout: app.py imports ai_music_therapy); pyproject.toml is the
# authoritative dependency list and the pins below mirror it.
-e .
streamlit>=1.48
openai>=2.0
pydantic>=2.10
pandas>=2.2
plotly>=5.24
python-dotenv>=1.0
psycopg[binary,pool]>=3.2
